//require 
describe('Global Tests for the Meadowlark Site', function(){
    it('each page has a valid title', function(){
	expect;
//	assert(document.title && document.title.match(/\S/) &&
	//	       document.title.toUpperCase() !== 'TODO');

    });
});
